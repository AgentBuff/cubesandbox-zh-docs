import{_ as c,C as h,o as s,c as p,a0 as i,b as r,w as o,a,E as d,a1 as l,j as t}from"./chunks/framework.CWk8AOcY.js";const x=JSON.parse('{"title":"Network (CubeVS)","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/network.md","filePath":"architecture/network.md"}'),u={name:"architecture/network.md"};function b(m,e,g,f,T,P){const n=h("Mermaid");return s(),p("div",null,[e[4]||(e[4]=i('<h1 id="network-cubevs" tabindex="-1">Network (CubeVS) <a class="header-anchor" href="#network-cubevs" aria-label="Permalink to &quot;Network (CubeVS)&quot;">​</a></h1><p>Cube-Sandbox isolates each sandbox with its own virtual network, giving every instance private, low-latency connectivity to the outside world while enforcing per-sandbox security policies entirely in kernel space. The subsystem that makes this possible is <strong>CubeVS</strong> -- a purpose-built network virtualization layer composed of three eBPF programs, a set of shared BPF maps, and a Go control-plane library.</p><p>This document explains the architecture, traffic paths, NAT model, policy engine, and lifecycle management that together form the CubeVS network.</p><hr><h2 id="_1-architecture-overview" tabindex="-1">1. Architecture Overview <a class="header-anchor" href="#_1-architecture-overview" aria-label="Permalink to &quot;1. Architecture Overview&quot;">​</a></h2><h3 id="_1-1-design-goals" tabindex="-1">1.1 Design Goals <a class="header-anchor" href="#_1-1-design-goals" aria-label="Permalink to &quot;1.1 Design Goals&quot;">​</a></h3><p>Traditional container networking stacks (Linux Bridge, OVS, iptables-based NAT) add per-packet overhead that grows with the number of tenants on a host. CubeVS replaces that stack with three small, cooperating eBPF programs attached at strategic points in the kernel data path:</p><ul><li><strong>Point-to-point low latency</strong> -- Each sandbox communicates through a dedicated TAP device; there is no shared bridge and no software switch hop.</li><li><strong>Kernel-space policy enforcement</strong> -- Network policies are evaluated in eBPF before a packet ever reaches userspace, keeping CPU overhead minimal.</li><li><strong>Scalable NAT</strong> -- SNAT port allocation uses a lock-protected pool with collision-resistant insertion, avoiding the iptables rule explosion that plagues large deployments.</li></ul><h3 id="_1-2-the-three-bpf-programs" tabindex="-1">1.2 The Three BPF Programs <a class="header-anchor" href="#_1-2-the-three-bpf-programs" aria-label="Permalink to &quot;1.2 The Three BPF Programs&quot;">​</a></h3><p>CubeVS attaches one BPF program to each of the three network boundaries a packet can cross on a host:</p><table tabindex="0"><thead><tr><th>Program</th><th>File</th><th>Attach Point</th><th>Direction</th><th>Role</th></tr></thead><tbody><tr><td><code>from_cube</code></td><td><code>mvmtap.bpf.c</code></td><td>TC ingress on each TAP device</td><td>Sandbox --&gt; Host</td><td>SNAT, policy check, session creation, ARP proxy</td></tr><tr><td><code>from_world</code></td><td><code>nodenic.bpf.c</code></td><td>TC ingress on host NIC (eth0)</td><td>External --&gt; Host</td><td>Reverse NAT, port-mapping proxy</td></tr><tr><td><code>from_envoy</code></td><td><code>localgw.bpf.c</code></td><td>TC egress on cube-dev</td><td>Overlay --&gt; Sandbox</td><td>DNAT overlay traffic to sandbox IP, redirect to TAP</td></tr></tbody></table><p>A fourth, lightweight program -- <code>filter_from_cube</code> -- is attached via XDP on cube-dev. It serves as an early-reject filter, dropping unsolicited inbound TCP and UDP packets before they enter the TC layer, while still allowing ICMP and packets that belong to existing sessions.</p><h3 id="_1-3-the-go-control-plane" tabindex="-1">1.3 The Go Control Plane <a class="header-anchor" href="#_1-3-the-go-control-plane" aria-label="Permalink to &quot;1.3 The Go Control Plane&quot;">​</a></h3><p>The <code>cubevs/</code> Go package wraps the BPF lifecycle:</p><ul><li><strong><code>Init()</code></strong> loads and pins all three BPF object files, rewrites compile-time constants (IPs, MACs, interface indices), and attaches TC / XDP filters.</li><li><strong><code>AddTAPDevice()</code> / <code>DelTAPDevice()</code></strong> register and deregister sandbox TAP devices, including their metadata and network policies.</li><li><strong><code>AttachFilter()</code></strong> creates the clsact qdisc on a TAP device and attaches the <code>from_cube</code> TC filter.</li><li><strong><code>SetSNATIPs()</code></strong> populates the SNAT IP pool.</li><li><strong>Network policy and port-mapping APIs</strong> update the corresponding BPF maps at runtime.</li><li><strong>Session reaper</strong> runs as a background goroutine, periodically sweeping expired NAT sessions.</li></ul><h3 id="_1-4-bpf-maps" tabindex="-1">1.4 BPF Maps <a class="header-anchor" href="#_1-4-bpf-maps" aria-label="Permalink to &quot;1.4 BPF Maps&quot;">​</a></h3><p>Nine pinned BPF maps (under <code>/sys/fs/bpf/</code>) form the shared state between the three programs and the Go control plane:</p><table tabindex="0"><thead><tr><th>Map</th><th>Type</th><th>Key</th><th>Value</th><th>Purpose</th></tr></thead><tbody><tr><td><code>mvmip_to_ifindex</code></td><td>Hash</td><td>Sandbox IP</td><td>TAP ifindex</td><td>IP-to-device lookup</td></tr><tr><td><code>ifindex_to_mvmmeta</code></td><td>Hash</td><td>TAP ifindex</td><td>Sandbox metadata (IP, ID, version)</td><td>Device-to-metadata lookup</td></tr><tr><td><code>egress_sessions</code></td><td>Hash</td><td>5-tuple (sandbox side)</td><td>NAT session state</td><td>Track outbound connections</td></tr><tr><td><code>ingress_sessions</code></td><td>Hash</td><td>5-tuple (external side)</td><td>Reverse-lookup metadata</td><td>Map reply packets back to sandbox</td></tr><tr><td><code>snat_iplist</code></td><td>Array</td><td>Index (0--3)</td><td>SNAT IP entry (IP, ifindex, port waterline)</td><td>SNAT IP pool</td></tr><tr><td><code>allow_out</code></td><td>Hash-of-Maps</td><td>TAP ifindex</td><td>Inner LPM trie (destination CIDRs)</td><td>Per-sandbox allow list</td></tr><tr><td><code>deny_out</code></td><td>Hash-of-Maps</td><td>TAP ifindex</td><td>Inner LPM trie (destination CIDRs)</td><td>Per-sandbox deny list</td></tr><tr><td><code>remote_port_mapping</code></td><td>Hash</td><td>Host port</td><td>TAP ifindex + sandbox listen port</td><td>Inbound port forwarding</td></tr><tr><td><code>local_port_mapping</code></td><td>Hash</td><td>TAP ifindex + sandbox listen port</td><td>Host port</td><td>Outbound port-mapping optimization</td></tr></tbody></table><p>These maps are pinned so that programs loaded at different times (e.g., per-TAP filters loaded after <code>Init()</code>) can share state through the filesystem.</p><hr><h2 id="_2-traffic-flows" tabindex="-1">2. Traffic Flows <a class="header-anchor" href="#_2-traffic-flows" aria-label="Permalink to &quot;2. Traffic Flows&quot;">​</a></h2><h3 id="_2-1-egress-sandbox-to-external-network" tabindex="-1">2.1 Egress: Sandbox to External Network <a class="header-anchor" href="#_2-1-egress-sandbox-to-external-network" aria-label="Permalink to &quot;2.1 Egress: Sandbox to External Network&quot;">​</a></h3><p>When a sandbox process opens a connection to the internet, the packet takes the following path:</p>',23)),(s(),r(l,null,{default:o(()=>[d(n,{id:"mermaid-352",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20A%5B%22Sandbox%3Cbr%2F%3E169.254.68.6%3Asrc_port%22%5D%20--%3E%7Craw%20packet%7C%20B%5B%22TAP%20Device%22%5D%0A%20%20%20%20B%20--%3E%7CTC%20ingress%7C%20C%5B%22from_cube%3Cbr%2F%3E(mvmtap.bpf.c)%22%5D%0A%20%20%20%20C%20--%3E%7CSNAT%20%2B%20policy%20%2B%20session%7C%20D%5B%22Host%20NIC%20(eth0)%22%5D%0A%20%20%20%20D%20--%3E%20E%5B%22External%20Network%22%5D%0A"})]),fallback:o(()=>[...e[0]||(e[0]=[a(" Loading... ",-1)])]),_:1})),e[5]||(e[5]=i('<p><strong>Step by step:</strong></p><ol><li>The sandbox sends a packet with source IP <code>169.254.68.6</code> (the fixed internal address) and a source port chosen by its TCP/UDP stack.</li><li>The packet enters the TAP device and hits the <code>from_cube</code> TC ingress filter.</li><li><code>from_cube</code> checks whether the destination is the sandbox gateway (<code>169.254.68.5</code>). If so, this is overlay-bound traffic -- the filter DNATs the destination to cube-dev and redirects the packet there.</li><li>For all other destinations, <code>from_cube</code>: <ul><li><strong>Evaluates network policy</strong> against the destination IP (see <a href="#5-network-policy">Section 5</a>).</li><li><strong>Creates or updates a NAT session</strong> in <code>egress_sessions</code> and <code>ingress_sessions</code>.</li><li><strong>Performs SNAT</strong>: replaces the sandbox source IP and port with an IP from the SNAT pool and a dynamically allocated port, updating L3 and L4 checksums.</li><li><strong>Redirects</strong> the rewritten packet to the host NIC.</li></ul></li><li>The packet leaves the host toward the external network.</li></ol><h3 id="_2-2-ingress-external-network-to-sandbox" tabindex="-1">2.2 Ingress: External Network to Sandbox <a class="header-anchor" href="#_2-2-ingress-external-network-to-sandbox" aria-label="Permalink to &quot;2.2 Ingress: External Network to Sandbox&quot;">​</a></h3><p>Reply packets (and port-mapped inbound connections) arrive at the host NIC and are routed back to the correct sandbox:</p>',4)),(s(),r(l,null,{default:o(()=>[d(n,{id:"mermaid-411",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20A%5B%22External%20Network%22%5D%20--%3E%7Creply%20packet%7C%20B%5B%22Host%20NIC%20(eth0)%22%5D%0A%20%20%20%20B%20--%3E%7CTC%20ingress%7C%20C%5B%22from_world%3Cbr%2F%3E(nodenic.bpf.c)%22%5D%0A%20%20%20%20C%20--%3E%7Csession%20lookup%20%2B%20reverse%20NAT%7C%20D%5B%22TAP%20Device%22%5D%0A%20%20%20%20D%20--%3E%20E%5B%22Sandbox%3Cbr%2F%3E169.254.68.6%3Adst_port%22%5D%0A"})]),fallback:o(()=>[...e[1]||(e[1]=[a(" Loading... ",-1)])]),_:1})),e[6]||(e[6]=i('<p><code>from_world</code> handles two cases:</p><ul><li><strong>Session-based reverse NAT</strong> -- The filter looks up the packet&#39;s 5-tuple in <code>ingress_sessions</code>. If a match is found, it reconstructs the original sandbox-side 5-tuple, performs reverse DNAT (rewriting destination IP and port back to the sandbox&#39;s internal address), and redirects the packet to the correct TAP device.</li><li><strong>Port-mapped proxy</strong> -- If no session matches, the filter checks <code>remote_port_mapping</code> using the destination port. A match means this is an inbound connection to a service the sandbox is exposing. The filter DNATs the packet to the sandbox&#39;s listen port and redirects to the TAP.</li></ul><h3 id="_2-3-overlay-traffic-envoy-to-sandbox" tabindex="-1">2.3 Overlay Traffic: Envoy to Sandbox <a class="header-anchor" href="#_2-3-overlay-traffic-envoy-to-sandbox" aria-label="Permalink to &quot;2.3 Overlay Traffic: Envoy to Sandbox&quot;">​</a></h3><p>Traffic arriving from the overlay network (e.g., from a sidecar proxy) enters through cube-dev:</p>',4)),(s(),r(l,null,{default:o(()=>[d(n,{id:"mermaid-433",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20A%5B%22Overlay%20%2F%20Envoy%22%5D%20--%3E%7Cpacket%7C%20B%5B%22cube-dev%22%5D%0A%20%20%20%20B%20--%3E%7CTC%20egress%7C%20C%5B%22from_envoy%3Cbr%2F%3E(localgw.bpf.c)%22%5D%0A%20%20%20%20C%20--%3E%7CDNAT%20to%20sandbox%20IP%3Cbr%2F%3Eredirect%7C%20D%5B%22TAP%20Device%22%5D%0A%20%20%20%20D%20--%3E%20E%5B%22Sandbox%22%5D%0A"})]),fallback:o(()=>[...e[2]||(e[2]=[a(" Loading... ",-1)])]),_:1})),e[7]||(e[7]=i(`<p><code>from_envoy</code> rewrites the destination IP from the overlay address to the sandbox&#39;s internal IP (<code>169.254.68.6</code>), sets the source to the gateway IP (<code>169.254.68.5</code>), and redirects the packet to the appropriate TAP device by looking up <code>mvmip_to_ifindex</code>.</p><hr><h2 id="_3-session-tracking" tabindex="-1">3. Session Tracking <a class="header-anchor" href="#_3-session-tracking" aria-label="Permalink to &quot;3. Session Tracking&quot;">​</a></h2><p>CubeVS maintains stateful connection tracking so that reply packets can be correctly reverse-NATed and so that the system can detect and clean up stale connections.</p><h3 id="_3-1-dual-map-design" tabindex="-1">3.1 Dual-Map Design <a class="header-anchor" href="#_3-1-dual-map-design" aria-label="Permalink to &quot;3.1 Dual-Map Design&quot;">​</a></h3><p>Two maps work in tandem:</p><ul><li><strong><code>egress_sessions</code></strong> is the primary session table. The key is the original 5-tuple as seen from the sandbox side (sandbox IP, destination IP, sandbox port, destination port, protocol, and a version field for multi-version support). The value holds the full NAT state: SNAT IP and port, TAP ifindex, timestamps, TCP state, and a flag for active-close handling.</li><li><strong><code>ingress_sessions</code></strong> is the reverse-lookup table. The key is the 5-tuple as seen from the external side (external IP, node IP, external port, SNAT port, protocol). The value stores just enough information (sandbox IP, sandbox port, version) to reconstruct the <code>egress_sessions</code> key and perform the reverse NAT.</li></ul><p>This dual-map approach avoids duplicating the full session state in both directions while still enabling efficient O(1) lookups from either side of the connection.</p><h3 id="_3-2-tcp-state-machine" tabindex="-1">3.2 TCP State Machine <a class="header-anchor" href="#_3-2-tcp-state-machine" aria-label="Permalink to &quot;3.2 TCP State Machine&quot;">​</a></h3><p>CubeVS implements a TCP connection-tracking state machine modeled after the Linux kernel&#39;s <code>nf_conntrack</code>. It recognizes 11 states, including <code>SYN_SENT</code>, <code>SYN_RECV</code>, <code>ESTABLISHED</code>, <code>FIN_WAIT</code>, <code>CLOSE_WAIT</code>, <code>LAST_ACK</code>, <code>TIME_WAIT</code>, <code>CLOSE</code>, and <code>SYN_SENT2</code> (for simultaneous open). State transitions are driven by the TCP flags observed in both the original (sandbox-to-external) and reply (external-to-sandbox) directions.</p><p>This level of tracking is important for two reasons:</p><ol><li><strong>Accurate timeouts</strong> -- An <code>ESTABLISHED</code> session can safely live for hours (default: 3 hours), while a half-open <code>SYN_SENT</code> session should be reaped after 1 minute.</li><li><strong>Active-close detection</strong> -- When the sandbox initiates the close (sends the first FIN), the session enters <code>TIME_WAIT</code> and lingers briefly to absorb retransmissions. When the external side initiates, the session enters <code>CLOSE_WAIT</code> / <code>LAST_ACK</code> with shorter timeouts.</li></ol><h3 id="_3-3-udp-and-icmp-tracking" tabindex="-1">3.3 UDP and ICMP Tracking <a class="header-anchor" href="#_3-3-udp-and-icmp-tracking" aria-label="Permalink to &quot;3.3 UDP and ICMP Tracking&quot;">​</a></h3><p><strong>UDP</strong> uses a simple two-state model:</p><ul><li><code>UNREPLIED</code> -- set when the first outbound packet is seen.</li><li><code>REPLIED</code> -- set when the first reply arrives. Timeout extends from 30 seconds (unreplied) to 180 seconds (replied).</li></ul><p><strong>ICMP</strong> also uses two states (<code>UNREPLIED</code> / <code>REPLIED</code>) with a fixed 30-second timeout. The ICMP echo identifier serves as the &quot;port&quot; in the session key, so concurrent pings from the same sandbox are tracked independently.</p><h3 id="_3-4-session-reaper" tabindex="-1">3.4 Session Reaper <a class="header-anchor" href="#_3-4-session-reaper" aria-label="Permalink to &quot;3.4 Session Reaper&quot;">​</a></h3><p>A Go background goroutine runs every 5 seconds and walks the <code>egress_sessions</code> map. For each session it compares <code>now - access_time</code> against a state-specific timeout:</p><table tabindex="0"><thead><tr><th>Protocol</th><th>State</th><th>Timeout</th></tr></thead><tbody><tr><td>TCP</td><td>SYN_SENT, SYN_RECV</td><td>1 minute</td></tr><tr><td>TCP</td><td>ESTABLISHED</td><td>3 hours</td></tr><tr><td>TCP</td><td>FIN_WAIT, CLOSE_WAIT, LAST_ACK</td><td>1--2 minutes</td></tr><tr><td>TCP</td><td>TIME_WAIT, CLOSE</td><td>10 seconds</td></tr><tr><td>UDP</td><td>UNREPLIED</td><td>30 seconds</td></tr><tr><td>UDP</td><td>REPLIED</td><td>180 seconds</td></tr><tr><td>ICMP</td><td>Any</td><td>30 seconds</td></tr></tbody></table><p>When a session expires, the reaper deletes both the <code>egress_sessions</code> and <code>ingress_sessions</code> entries. If the session was not in a normal terminal state (e.g., <code>ESTABLISHED</code> timing out without a FIN), the reaper logs a warning. It also monitors session count and alerts when occupancy exceeds 80% of the map&#39;s capacity.</p><hr><h2 id="_4-snat-and-dnat" tabindex="-1">4. SNAT and DNAT <a class="header-anchor" href="#_4-snat-and-dnat" aria-label="Permalink to &quot;4. SNAT and DNAT&quot;">​</a></h2><h3 id="_4-1-snat-outbound-address-translation" tabindex="-1">4.1 SNAT: Outbound Address Translation <a class="header-anchor" href="#_4-1-snat-outbound-address-translation" aria-label="Permalink to &quot;4.1 SNAT: Outbound Address Translation&quot;">​</a></h3><p>Every outbound packet from a sandbox must be rewritten with a routable source address before it can leave the host. CubeVS performs this in <code>from_cube</code> using a pool of up to four SNAT IPs.</p><p><strong>IP selection</strong> is deterministic per sandbox: <code>index = jhash(sandbox_ip) % 4</code>. This ensures that all connections from the same sandbox use the same SNAT IP, which simplifies external firewall rules and logging.</p><p><strong>Port allocation</strong> works as a monotonically increasing waterline per SNAT IP entry. The waterline starts at port 30000 and increments with each new connection. When it reaches 65535 it wraps around. The SNAT entry is protected by a BPF spin lock so that concurrent allocations on different CPUs do not race.</p><p><strong>Collision avoidance</strong> is handled at insertion time. After selecting a port, the BPF program attempts to insert the reverse-lookup entry into <code>ingress_sessions</code> using the <code>BPF_NOEXIST</code> flag. If the key already exists (meaning another session is already using that SNAT IP:port combination for the same external endpoint), the allocator increments the port and retries, up to 10 attempts. If all 10 fail, the packet is dropped.</p><p>After a successful allocation, <code>from_cube</code> updates the IP header (source IP and checksum) and the transport header (source port and checksum) in place, then redirects the packet to the host NIC.</p><h3 id="_4-2-dnat-inbound-address-translation" tabindex="-1">4.2 DNAT: Inbound Address Translation <a class="header-anchor" href="#_4-2-dnat-inbound-address-translation" aria-label="Permalink to &quot;4.2 DNAT: Inbound Address Translation&quot;">​</a></h3><p>DNAT occurs in two contexts:</p><ol><li><p><strong>Overlay traffic</strong> (<code>from_envoy</code> on cube-dev) -- The destination IP is rewritten from the overlay address to the sandbox&#39;s internal IP (<code>169.254.68.6</code>), and the source is set to the gateway address (<code>169.254.68.5</code>). The packet is redirected to the correct TAP device.</p></li><li><p><strong>Session reply traffic</strong> (<code>from_world</code> on host NIC) -- The destination IP and port are rewritten from the node&#39;s SNAT address back to the sandbox&#39;s original source address and port. The reaper has not yet cleaned the session, so the reverse-lookup in <code>ingress_sessions</code> provides the sandbox-side coordinates.</p></li><li><p><strong>Port-mapped traffic</strong> (<code>from_world</code> on host NIC) -- For services exposed via port mapping, the destination is rewritten to the sandbox&#39;s listen port. No session table is involved; the <code>remote_port_mapping</code> map provides the translation directly.</p></li></ol><hr><h2 id="_5-network-policy" tabindex="-1">5. Network Policy <a class="header-anchor" href="#_5-network-policy" aria-label="Permalink to &quot;5. Network Policy&quot;">​</a></h2><p>CubeVS enforces per-sandbox outbound network policies entirely in kernel space, using LPM (Longest Prefix Match) tries for CIDR-based rules.</p><h3 id="_5-1-architecture" tabindex="-1">5.1 Architecture <a class="header-anchor" href="#_5-1-architecture" aria-label="Permalink to &quot;5.1 Architecture&quot;">​</a></h3><p>Each sandbox can have two LPM tries associated with its TAP device:</p><ul><li><strong><code>allow_out</code></strong> -- An allow list of destination CIDRs. If this map exists and the destination matches, the packet is allowed regardless of the deny list.</li><li><strong><code>deny_out</code></strong> -- A deny list of destination CIDRs. If this map exists and the destination matches (and it was not in the allow list), the packet is dropped.</li></ul><p>Both are implemented as hash-of-maps: the outer map is keyed by TAP ifindex, and each value is a file descriptor to an inner LPM trie. This per-device structure means that policy updates for one sandbox do not require iterating over or locking maps belonging to other sandboxes.</p><h3 id="_5-2-evaluation-order" tabindex="-1">5.2 Evaluation Order <a class="header-anchor" href="#_5-2-evaluation-order" aria-label="Permalink to &quot;5.2 Evaluation Order&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. Is the destination the sandbox gateway (169.254.68.5)?</span></span>
<span class="line"><span>   --&gt; YES: Allow (internal traffic, always permitted)</span></span>
<span class="line"><span>   </span></span>
<span class="line"><span>2. Does the sandbox have an allow_out map, and does the destination match?</span></span>
<span class="line"><span>   --&gt; YES: Allow</span></span>
<span class="line"><span>   </span></span>
<span class="line"><span>3. Does the sandbox have a deny_out map, and does the destination match?</span></span>
<span class="line"><span>   --&gt; YES: Drop</span></span>
<span class="line"><span>   </span></span>
<span class="line"><span>4. Default: Allow</span></span></code></pre></div><p>The priority is: <strong>allow &gt; deny &gt; default-allow</strong>. This means a sandbox operator can set a broad deny rule (e.g., <code>0.0.0.0/0</code> to block all internet access) and then punch specific holes with allow rules.</p><h3 id="_5-3-always-denied-cidrs" tabindex="-1">5.3 Always-Denied CIDRs <a class="header-anchor" href="#_5-3-always-denied-cidrs" aria-label="Permalink to &quot;5.3 Always-Denied CIDRs&quot;">​</a></h3><p>Regardless of policy configuration, CubeVS prevents sandboxes from reaching private and link-local address ranges:</p><ul><li><code>10.0.0.0/8</code></li><li><code>127.0.0.0/8</code></li><li><code>169.254.0.0/16</code></li><li><code>172.16.0.0/12</code></li><li><code>192.168.0.0/16</code></li></ul><p>These ranges are added to the deny list during <code>AddTAPDevice()</code> and cannot be overridden by allow rules. This ensures that a sandbox cannot probe the host&#39;s internal network or other sandboxes&#39; link-local addresses.</p><h3 id="_5-4-policy-configuration" tabindex="-1">5.4 Policy Configuration <a class="header-anchor" href="#_5-4-policy-configuration" aria-label="Permalink to &quot;5.4 Policy Configuration&quot;">​</a></h3><p>When a TAP device is registered, the caller provides an <code>MVMOptions</code> struct:</p><ul><li><strong><code>AllowInternetAccess</code></strong> -- If <code>false</code>, a blanket deny rule (<code>0.0.0.0/0</code>) is installed.</li><li><strong><code>AllowOut</code></strong> -- A list of CIDRs to add to the allow list.</li><li><strong><code>DenyOut</code></strong> -- A list of CIDRs to add to the deny list.</li></ul><p>Policies can be updated at runtime by modifying the inner LPM tries without detaching or reloading BPF programs.</p><hr><h2 id="_6-port-mapping" tabindex="-1">6. Port Mapping <a class="header-anchor" href="#_6-port-mapping" aria-label="Permalink to &quot;6. Port Mapping&quot;">​</a></h2><p>Sandboxes are not directly reachable from outside the host. When a sandbox needs to expose a service (e.g., an HTTP server), CubeVS provides port mapping -- a static NAT rule that forwards traffic arriving at a specific host port to a specific sandbox port.</p><h3 id="_6-1-dual-mappings" tabindex="-1">6.1 Dual Mappings <a class="header-anchor" href="#_6-1-dual-mappings" aria-label="Permalink to &quot;6.1 Dual Mappings&quot;">​</a></h3><p>Two BPF maps support port mapping:</p><ul><li><strong><code>remote_port_mapping</code></strong> maps a host port to a (TAP ifindex, sandbox listen port) pair. This is used by <code>from_world</code> to route inbound connections to the right sandbox.</li><li><strong><code>local_port_mapping</code></strong> maps (TAP ifindex, sandbox listen port) back to the host port. This is used by <code>from_cube</code> as an optimization: when a sandbox sends a packet from a mapped listen port, the filter can skip full NAT session creation and directly SNAT the packet to the node IP with the correct port, then redirect to the host NIC.</li></ul><h3 id="_6-2-inbound-proxy-flow" tabindex="-1">6.2 Inbound Proxy Flow <a class="header-anchor" href="#_6-2-inbound-proxy-flow" aria-label="Permalink to &quot;6.2 Inbound Proxy Flow&quot;">​</a></h3><ol><li>An external client sends a packet to <code>node_ip:host_port</code>.</li><li><code>from_world</code> looks up <code>remote_port_mapping[host_port]</code>.</li><li>On a match, the filter rewrites the destination to <code>169.254.68.6:sandbox_listen_port</code> and redirects to the TAP device.</li><li>The sandbox receives the connection on its listen port.</li></ol><p>This path does not create entries in the session tables, which keeps the maps lean for long-lived service connections.</p><h3 id="_6-3-management" tabindex="-1">6.3 Management <a class="header-anchor" href="#_6-3-management" aria-label="Permalink to &quot;6.3 Management&quot;">​</a></h3><p>The Go API provides <code>AddPortMapping()</code>, <code>DelPortMapping()</code>, <code>ListPortMapping()</code>, and <code>GetPortMapping()</code> for managing mappings at runtime.</p><hr><h2 id="_7-tap-device-lifecycle" tabindex="-1">7. TAP Device Lifecycle <a class="header-anchor" href="#_7-tap-device-lifecycle" aria-label="Permalink to &quot;7. TAP Device Lifecycle&quot;">​</a></h2><p>Each sandbox gets a dedicated TAP device that serves as its sole network interface on the host side. CubeVS manages the full lifecycle of these devices.</p><h3 id="_7-1-registration-addtapdevice" tabindex="-1">7.1 Registration: AddTAPDevice <a class="header-anchor" href="#_7-1-registration-addtapdevice" aria-label="Permalink to &quot;7.1 Registration: AddTAPDevice&quot;">​</a></h3><p>When the sandbox runtime (Cubelet) creates a new sandbox, it calls <code>AddTAPDevice(ifindex, ip, id, version, options)</code>:</p><ol><li>The sandbox&#39;s metadata (IP, UUID, version) is written to <code>ifindex_to_mvmmeta</code>.</li><li>The IP-to-device mapping is written to <code>mvmip_to_ifindex</code>.</li><li>Network policy inner maps are initialized based on <code>MVMOptions</code> -- always-denied CIDRs are installed first, then any caller-specified allow or deny rules.</li></ol><h3 id="_7-2-filter-attachment-attachfilter" tabindex="-1">7.2 Filter Attachment: AttachFilter <a class="header-anchor" href="#_7-2-filter-attachment-attachfilter" aria-label="Permalink to &quot;7.2 Filter Attachment: AttachFilter&quot;">​</a></h3><p>After the TAP device is created at the OS level, <code>AttachFilter(ifindex)</code> loads the pinned <code>from_cube</code> BPF program and attaches it:</p><ol><li>A clsact qdisc is created on the TAP device (if one does not already exist).</li><li>The <code>from_cube</code> TC filter is attached to the ingress hook of the qdisc.</li><li>The inner LPM tries for <code>allow_out</code> and <code>deny_out</code> are created and linked to the outer hash-of-maps.</li></ol><p>From this point on, every packet the sandbox sends is intercepted by <code>from_cube</code>.</p><h3 id="_7-3-teardown-deltapdevice" tabindex="-1">7.3 Teardown: DelTAPDevice <a class="header-anchor" href="#_7-3-teardown-deltapdevice" aria-label="Permalink to &quot;7.3 Teardown: DelTAPDevice&quot;">​</a></h3><p>When a sandbox is destroyed, <code>DelTAPDevice(ifindex, ip)</code>:</p><ol><li>Removes the network policy inner maps (the inner LPM tries are deleted from the hash-of-maps).</li><li>Deletes the <code>ifindex_to_mvmmeta</code> and <code>mvmip_to_ifindex</code> entries.</li><li>Any active sessions referencing this TAP device are left in place -- the session reaper will clean them up when they time out. This avoids the need for an expensive full-map scan at teardown time.</li></ol><hr><h2 id="_8-initialization" tabindex="-1">8. Initialization <a class="header-anchor" href="#_8-initialization" aria-label="Permalink to &quot;8. Initialization&quot;">​</a></h2><h3 id="_8-1-the-init-flow" tabindex="-1">8.1 The Init() Flow <a class="header-anchor" href="#_8-1-the-init-flow" aria-label="Permalink to &quot;8.1 The Init() Flow&quot;">​</a></h3><p><code>Init()</code> is called once when the network agent starts. It performs the system-level setup that must happen before any sandboxes can be created:</p><ol><li><p><strong>Load BPF objects</strong> -- The three object files (localgw, mvmtap, nodenic) are loaded into the kernel. Before loading, the Go library rewrites compile-time constants in the BPF bytecode with the actual values for this host:</p><ul><li>Sandbox internal IP (<code>169.254.68.6</code>) and gateway IP (<code>169.254.68.5</code>)</li><li>Sandbox MAC address</li><li>cube-dev interface index, IP, and MAC</li><li>Host NIC interface index, IP, MAC, and next-hop gateway MAC</li></ul></li><li><p><strong>Pin programs and maps</strong> -- All programs and maps are pinned under <code>/sys/fs/bpf/</code> so that programs loaded later (per-TAP filters) can access the shared maps by path.</p></li><li><p><strong>Attach TC filters</strong> --</p><ul><li><code>from_envoy</code> is attached to cube-dev&#39;s TC egress hook (handling overlay-to-sandbox traffic).</li><li><code>from_world</code> is attached to the host NIC&#39;s TC ingress hook (handling external-to-sandbox reply traffic).</li><li><code>from_world</code> is also attached to the loopback interface for completeness.</li></ul></li><li><p><strong>Attach XDP</strong> -- <code>filter_from_cube</code> is attached to cube-dev&#39;s XDP hook. This provides an early, high-performance rejection of unsolicited inbound TCP/UDP traffic before it reaches the TC layer.</p></li></ol><h3 id="_8-2-constant-rewriting" tabindex="-1">8.2 Constant Rewriting <a class="header-anchor" href="#_8-2-constant-rewriting" aria-label="Permalink to &quot;8.2 Constant Rewriting&quot;">​</a></h3><p>BPF programs cannot read configuration files at runtime. Instead, CubeVS uses a pattern common in the eBPF ecosystem: global variables in the BPF C source are compiled as constants, and the Go loader rewrites their values in the ELF object before loading it into the kernel. This approach gives the programs the performance of compile-time constants (the verifier can optimize branches) with the flexibility of runtime configuration.</p><hr><h2 id="_9-arp-proxy" tabindex="-1">9. ARP Proxy <a class="header-anchor" href="#_9-arp-proxy" aria-label="Permalink to &quot;9. ARP Proxy&quot;">​</a></h2><p>Sandboxes are assigned a link-local IP (<code>169.254.68.6</code>) and use <code>169.254.68.5</code> as their default gateway. Since these addresses exist only inside the TAP device&#39;s point-to-point link, there is no real host at <code>169.254.68.5</code> to respond to ARP requests. CubeVS solves this with an ARP proxy built into the <code>from_cube</code> filter.</p><h3 id="_9-1-how-it-works" tabindex="-1">9.1 How It Works <a class="header-anchor" href="#_9-1-how-it-works" aria-label="Permalink to &quot;9.1 How It Works&quot;">​</a></h3><ol><li>The sandbox&#39;s network stack sends an ARP request: <em>&quot;Who has 169.254.68.5? Tell 169.254.68.6.&quot;</em></li><li><code>from_cube</code> detects the ARP request, swaps the sender and target IP addresses to form an ARP reply, and fills in the sender MAC with the cube-dev gateway MAC address.</li><li>The reply is redirected back out the same TAP device (egress direction), arriving at the sandbox as if a real gateway had responded.</li></ol><p>The sandbox now has a valid ARP entry for its default gateway and can send IP packets to any destination. Those packets are routed through the TAP, intercepted by <code>from_cube</code>, SNATed, and forwarded to the host NIC -- completing the egress path described in Section 2.1.</p><h3 id="_9-2-why-an-arp-proxy" tabindex="-1">9.2 Why an ARP Proxy <a class="header-anchor" href="#_9-2-why-an-arp-proxy" aria-label="Permalink to &quot;9.2 Why an ARP Proxy&quot;">​</a></h3><p>In a traditional bridge-based setup, ARP is resolved by the bridge forwarding requests to all ports. CubeVS does not use a bridge; each TAP is an isolated point-to-point link. The ARP proxy is the minimal mechanism needed to make IP routing work over this link without introducing any broadcast domain or shared L2 segment.</p><hr><h2 id="_10-summary" tabindex="-1">10. Summary <a class="header-anchor" href="#_10-summary" aria-label="Permalink to &quot;10. Summary&quot;">​</a></h2><p>CubeVS achieves sandbox network isolation through a three-layer eBPF architecture:</p>`,91)),(s(),r(l,null,{default:o(()=>[d(n,{id:"mermaid-1010",class:"mermaid",graph:"graph%20TB%0A%20%20%20%20subgraph%20%22Overlay%20Network%22%0A%20%20%20%20%20%20%20%20OV%5B%22Envoy%20%2F%20Overlay%22%5D%0A%20%20%20%20end%0A%20%20%20%20%0A%20%20%20%20subgraph%20%22Host%20Kernel%22%0A%20%20%20%20%20%20%20%20CD%5B%22cube-dev%3Cbr%2F%3E(from_envoy%20%2B%20XDP%20filter)%22%5D%0A%20%20%20%20%20%20%20%20NIC%5B%22Host%20NIC%20-%20eth0%3Cbr%2F%3E(from_world)%22%5D%0A%20%20%20%20end%0A%20%20%20%20%0A%20%20%20%20subgraph%20%22Sandbox%20TAP%20Devices%22%0A%20%20%20%20%20%20%20%20T1%5B%22TAP%201%3Cbr%2F%3E(from_cube)%22%5D%0A%20%20%20%20%20%20%20%20T2%5B%22TAP%202%3Cbr%2F%3E(from_cube)%22%5D%0A%20%20%20%20%20%20%20%20T3%5B%22TAP%20N%3Cbr%2F%3E(from_cube)%22%5D%0A%20%20%20%20end%0A%20%20%20%20%0A%20%20%20%20subgraph%20%22Sandboxes%22%0A%20%20%20%20%20%20%20%20S1%5B%22Sandbox%201%22%5D%0A%20%20%20%20%20%20%20%20S2%5B%22Sandbox%202%22%5D%0A%20%20%20%20%20%20%20%20S3%5B%22Sandbox%20N%22%5D%0A%20%20%20%20end%0A%20%20%20%20%0A%20%20%20%20OV%20--%3E%20CD%0A%20%20%20%20CD%20--%3E%20T1%20%26%20T2%20%26%20T3%0A%20%20%20%20T1%20--%3E%20S1%0A%20%20%20%20T2%20--%3E%20S2%0A%20%20%20%20T3%20--%3E%20S3%0A%20%20%20%20S1%20--%3E%20T1%0A%20%20%20%20S2%20--%3E%20T2%0A%20%20%20%20S3%20--%3E%20T3%0A%20%20%20%20T1%20%26%20T2%20%26%20T3%20--%3E%7CSNAT%7C%20NIC%0A%20%20%20%20NIC%20--%3E%7CReverse%20NAT%7C%20T1%20%26%20T2%20%26%20T3%0A%20%20%20%20NIC%20--%3E%20EXT%5B%22External%20Network%22%5D%0A%20%20%20%20EXT%20--%3E%20NIC%0A"})]),fallback:o(()=>[...e[3]||(e[3]=[a(" Loading... ",-1)])]),_:1})),e[8]||(e[8]=t("p",null,"The key design principles are:",-1)),e[9]||(e[9]=t("ul",null,[t("li",null,[t("strong",null,"All data-path logic in kernel space"),a(" -- Policy evaluation, NAT, session tracking, and ARP resolution happen in eBPF with no context switches to userspace.")]),t("li",null,[t("strong",null,"Per-sandbox isolation"),a(" -- Each sandbox has its own TAP device, its own policy tries, and its own sessions. There is no shared bridge or switch.")]),t("li",null,[t("strong",null,"Stateful connection tracking"),a(" -- A dual-map session model (egress + ingress) enables bidirectional NAT with O(1) lookups and accurate protocol-aware timeouts.")]),t("li",null,[t("strong",null,"Minimal control plane"),a(" -- The Go library handles initialization, device lifecycle, and periodic session cleanup. The steady-state data path is entirely in eBPF.")])],-1))])}const _=c(u,[["render",b]]);export{x as __pageData,_ as default};

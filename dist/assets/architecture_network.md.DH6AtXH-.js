import{_ as s,C as p,o as a,c as A,a0 as i,b as l,w as d,a as t,E as c,a1 as n,j as o}from"./chunks/framework.CWk8AOcY.js";const C=JSON.parse('{"title":"网络模型 (CubeVS)","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/network.md","filePath":"zh/architecture/network.md"}'),h={name:"architecture/network.md"};function P(_,e,u,T,E,b){const r=p("Mermaid");return a(),A("div",null,[e[4]||(e[4]=i('<h1 id="网络模型-cubevs" tabindex="-1">网络模型 (CubeVS) <a class="header-anchor" href="#网络模型-cubevs" aria-label="Permalink to &quot;网络模型 (CubeVS)&quot;">​</a></h1><p>Cube-Sandbox 为每个沙箱提供独立的虚拟网络，使其拥有私有且低延迟的外部连接能力，同时完全在内核态执行逐沙箱的安全策略。实现这一切的子系统称为 <strong>CubeVS</strong> —— 一个专为沙箱场景设计的网络虚拟化层，由三个 eBPF 程序、一组共享的 BPF Map 和一个 Go 控制面库组成。</p><p>本文档介绍 CubeVS 的整体架构、流量路径、NAT 模型、策略引擎以及设备生命周期管理。</p><hr><h2 id="_1-架构概览" tabindex="-1">1. 架构概览 <a class="header-anchor" href="#_1-架构概览" aria-label="Permalink to &quot;1. 架构概览&quot;">​</a></h2><h3 id="_1-1-设计目标" tabindex="-1">1.1 设计目标 <a class="header-anchor" href="#_1-1-设计目标" aria-label="Permalink to &quot;1.1 设计目标&quot;">​</a></h3><p>传统的容器网络方案（Linux Bridge、OVS、基于 iptables 的 NAT）在每个报文上都会引入额外开销，并且开销会随主机上租户数量的增长而加剧。CubeVS 用三个精简的 eBPF 程序取代了这一整套网络栈，它们被挂载在内核数据路径的关键位置上，协同工作：</p><ul><li><strong>点对点低延迟</strong> —— 每个沙箱通过专属的 TAP 设备通信，没有共享网桥，也没有软件交换机的额外一跳。</li><li><strong>内核态策略执行</strong> —— 网络策略在 eBPF 中完成评估，报文无需到达用户态，CPU 开销极低。</li><li><strong>可扩展的 NAT</strong> —— SNAT 端口分配使用带自旋锁保护的资源池和防碰撞插入机制，避免了大规模部署中 iptables 规则膨胀的问题。</li></ul><h3 id="_1-2-三个-bpf-程序" tabindex="-1">1.2 三个 BPF 程序 <a class="header-anchor" href="#_1-2-三个-bpf-程序" aria-label="Permalink to &quot;1.2 三个 BPF 程序&quot;">​</a></h3><p>CubeVS 在报文可能经过的三个网络边界上各挂载一个 BPF 程序：</p><table tabindex="0"><thead><tr><th>程序</th><th>源文件</th><th>挂载点</th><th>方向</th><th>职责</th></tr></thead><tbody><tr><td><code>from_cube</code></td><td><code>mvmtap.bpf.c</code></td><td>各 TAP 设备的 TC ingress</td><td>沙箱 --&gt; 宿主机</td><td>SNAT、策略检查、会话创建、ARP 代理</td></tr><tr><td><code>from_world</code></td><td><code>nodenic.bpf.c</code></td><td>宿主机网卡 (eth0) 的 TC ingress</td><td>外部 --&gt; 宿主机</td><td>反向 NAT、端口映射代理</td></tr><tr><td><code>from_envoy</code></td><td><code>localgw.bpf.c</code></td><td>cube-dev 的 TC egress</td><td>Overlay --&gt; 沙箱</td><td>将 Overlay 流量 DNAT 到沙箱 IP 并重定向至 TAP</td></tr></tbody></table><p>此外还有一个轻量程序 <code>filter_from_cube</code>，通过 XDP 挂载在 cube-dev 上。它作为早期拒绝过滤器，在报文进入 TC 层之前丢弃未经请求的入站 TCP/UDP 报文，同时放行 ICMP 和属于已有会话的报文。</p><h3 id="_1-3-go-控制面" tabindex="-1">1.3 Go 控制面 <a class="header-anchor" href="#_1-3-go-控制面" aria-label="Permalink to &quot;1.3 Go 控制面&quot;">​</a></h3><p><code>cubevs/</code> Go 包封装了 BPF 的生命周期管理：</p><ul><li><strong><code>Init()</code></strong> 加载并固定三个 BPF 目标文件，改写编译期常量（IP、MAC、接口索引），并挂载 TC / XDP 过滤器。</li><li><strong><code>AddTAPDevice()</code> / <code>DelTAPDevice()</code></strong> 注册和注销沙箱 TAP 设备，包括其元数据和网络策略。</li><li><strong><code>AttachFilter()</code></strong> 在 TAP 设备上创建 clsact qdisc，并挂载 <code>from_cube</code> TC 过滤器。</li><li><strong><code>SetSNATIPs()</code></strong> 填充 SNAT IP 池。</li><li><strong>网络策略与端口映射 API</strong> 在运行时更新对应的 BPF Map。</li><li><strong>会话回收器</strong> 以后台 goroutine 方式运行，定期清理过期的 NAT 会话。</li></ul><h3 id="_1-4-bpf-map" tabindex="-1">1.4 BPF Map <a class="header-anchor" href="#_1-4-bpf-map" aria-label="Permalink to &quot;1.4 BPF Map&quot;">​</a></h3><p>九个固定在 <code>/sys/fs/bpf/</code> 下的 BPF Map 构成了三个程序与 Go 控制面之间的共享状态：</p><table tabindex="0"><thead><tr><th>Map</th><th>类型</th><th>Key</th><th>Value</th><th>用途</th></tr></thead><tbody><tr><td><code>mvmip_to_ifindex</code></td><td>Hash</td><td>沙箱 IP</td><td>TAP ifindex</td><td>IP 到设备的映射</td></tr><tr><td><code>ifindex_to_mvmmeta</code></td><td>Hash</td><td>TAP ifindex</td><td>沙箱元数据（IP、ID、版本）</td><td>设备到元数据的映射</td></tr><tr><td><code>egress_sessions</code></td><td>Hash</td><td>五元组（沙箱侧）</td><td>NAT 会话状态</td><td>跟踪出站连接</td></tr><tr><td><code>ingress_sessions</code></td><td>Hash</td><td>五元组（外部侧）</td><td>反向查找元数据</td><td>将回复报文映射回沙箱</td></tr><tr><td><code>snat_iplist</code></td><td>Array</td><td>索引（0--3）</td><td>SNAT IP 条目（IP、ifindex、端口水位线）</td><td>SNAT IP 池</td></tr><tr><td><code>allow_out</code></td><td>Hash-of-Maps</td><td>TAP ifindex</td><td>内部 LPM Trie（目标 CIDR）</td><td>逐沙箱允许列表</td></tr><tr><td><code>deny_out</code></td><td>Hash-of-Maps</td><td>TAP ifindex</td><td>内部 LPM Trie（目标 CIDR）</td><td>逐沙箱拒绝列表</td></tr><tr><td><code>remote_port_mapping</code></td><td>Hash</td><td>宿主端口</td><td>TAP ifindex + 沙箱监听端口</td><td>入站端口转发</td></tr><tr><td><code>local_port_mapping</code></td><td>Hash</td><td>TAP ifindex + 沙箱监听端口</td><td>宿主端口</td><td>出站端口映射优化</td></tr></tbody></table><p>这些 Map 通过文件系统固定，使得在不同时间加载的程序（例如 <code>Init()</code> 之后才加载的逐 TAP 过滤器）能够共享状态。</p><hr><h2 id="_2-流量路径" tabindex="-1">2. 流量路径 <a class="header-anchor" href="#_2-流量路径" aria-label="Permalink to &quot;2. 流量路径&quot;">​</a></h2><h3 id="_2-1-出站-沙箱到外部网络" tabindex="-1">2.1 出站：沙箱到外部网络 <a class="header-anchor" href="#_2-1-出站-沙箱到外部网络" aria-label="Permalink to &quot;2.1 出站：沙箱到外部网络&quot;">​</a></h3><p>当沙箱进程发起到互联网的连接时，报文经过以下路径：</p>',23)),(a(),l(n,null,{default:d(()=>[c(r,{id:"mermaid-352",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20A%5B%22%E6%B2%99%E7%AE%B1%3Cbr%2F%3E169.254.68.6%3Asrc_port%22%5D%20--%3E%7C%E5%8E%9F%E5%A7%8B%E6%8A%A5%E6%96%87%7C%20B%5B%22TAP%20%E8%AE%BE%E5%A4%87%22%5D%0A%20%20%20%20B%20--%3E%7CTC%20ingress%7C%20C%5B%22from_cube%3Cbr%2F%3E(mvmtap.bpf.c)%22%5D%0A%20%20%20%20C%20--%3E%7CSNAT%20%2B%20%E7%AD%96%E7%95%A5%20%2B%20%E4%BC%9A%E8%AF%9D%7C%20D%5B%22%E5%AE%BF%E4%B8%BB%E6%9C%BA%E7%BD%91%E5%8D%A1%20(eth0)%22%5D%0A%20%20%20%20D%20--%3E%20E%5B%22%E5%A4%96%E9%83%A8%E7%BD%91%E7%BB%9C%22%5D%0A"})]),fallback:d(()=>[...e[0]||(e[0]=[t(" Loading... ",-1)])]),_:1})),e[5]||(e[5]=i('<p><strong>详细步骤：</strong></p><ol><li>沙箱发送报文，源 IP 为 <code>169.254.68.6</code>（固定内部地址），源端口由其 TCP/UDP 栈选择。</li><li>报文进入 TAP 设备，命中 <code>from_cube</code> TC ingress 过滤器。</li><li><code>from_cube</code> 首先检查目标是否为沙箱网关（<code>169.254.68.5</code>）。若是，说明这是 Overlay 流量 —— 过滤器将目标 DNAT 到 cube-dev 并重定向报文。</li><li>对于其他目标，<code>from_cube</code> 依次执行： <ul><li><strong>评估网络策略</strong>，根据目标 IP 决定放行或丢弃（详见<a href="#5-网络策略">第 5 节</a>）。</li><li><strong>创建或更新 NAT 会话</strong>，写入 <code>egress_sessions</code> 和 <code>ingress_sessions</code>。</li><li><strong>执行 SNAT</strong>：将沙箱源 IP 和端口替换为 SNAT 池中的 IP 及动态分配的端口，同时更新 L3 和 L4 校验和。</li><li><strong>重定向</strong>改写后的报文到宿主机网卡。</li></ul></li><li>报文离开宿主机，发往外部网络。</li></ol><h3 id="_2-2-入站-外部网络到沙箱" tabindex="-1">2.2 入站：外部网络到沙箱 <a class="header-anchor" href="#_2-2-入站-外部网络到沙箱" aria-label="Permalink to &quot;2.2 入站：外部网络到沙箱&quot;">​</a></h3><p>回复报文（以及端口映射的入站连接）到达宿主机网卡，被路由回正确的沙箱：</p>',4)),(a(),l(n,null,{default:d(()=>[c(r,{id:"mermaid-411",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20A%5B%22%E5%A4%96%E9%83%A8%E7%BD%91%E7%BB%9C%22%5D%20--%3E%7C%E5%9B%9E%E5%A4%8D%E6%8A%A5%E6%96%87%7C%20B%5B%22%E5%AE%BF%E4%B8%BB%E6%9C%BA%E7%BD%91%E5%8D%A1%20(eth0)%22%5D%0A%20%20%20%20B%20--%3E%7CTC%20ingress%7C%20C%5B%22from_world%3Cbr%2F%3E(nodenic.bpf.c)%22%5D%0A%20%20%20%20C%20--%3E%7C%E4%BC%9A%E8%AF%9D%E6%9F%A5%E6%89%BE%20%2B%20%E5%8F%8D%E5%90%91%20NAT%7C%20D%5B%22TAP%20%E8%AE%BE%E5%A4%87%22%5D%0A%20%20%20%20D%20--%3E%20E%5B%22%E6%B2%99%E7%AE%B1%3Cbr%2F%3E169.254.68.6%3Adst_port%22%5D%0A"})]),fallback:d(()=>[...e[1]||(e[1]=[t(" Loading... ",-1)])]),_:1})),e[6]||(e[6]=i('<p><code>from_world</code> 处理两种情况：</p><ul><li><strong>基于会话的反向 NAT</strong> —— 过滤器在 <code>ingress_sessions</code> 中查找报文的五元组。若匹配成功，则重建沙箱侧的原始五元组，执行反向 DNAT（将目标 IP 和端口改写回沙箱内部地址），并重定向报文到正确的 TAP 设备。</li><li><strong>端口映射代理</strong> —— 若会话未匹配，过滤器根据目标端口查找 <code>remote_port_mapping</code>。若匹配成功，说明这是沙箱对外暴露服务的入站连接。过滤器将目标 DNAT 到沙箱的监听端口并重定向至 TAP。</li></ul><h3 id="_2-3-overlay-流量-envoy-到沙箱" tabindex="-1">2.3 Overlay 流量：Envoy 到沙箱 <a class="header-anchor" href="#_2-3-overlay-流量-envoy-到沙箱" aria-label="Permalink to &quot;2.3 Overlay 流量：Envoy 到沙箱&quot;">​</a></h3><p>来自 Overlay 网络的流量（例如来自 Sidecar 代理）通过 cube-dev 进入：</p>',4)),(a(),l(n,null,{default:d(()=>[c(r,{id:"mermaid-433",class:"mermaid",graph:"graph%20LR%0A%20%20%20%20A%5B%22Overlay%20%2F%20Envoy%22%5D%20--%3E%7C%E6%8A%A5%E6%96%87%7C%20B%5B%22cube-dev%22%5D%0A%20%20%20%20B%20--%3E%7CTC%20egress%7C%20C%5B%22from_envoy%3Cbr%2F%3E(localgw.bpf.c)%22%5D%0A%20%20%20%20C%20--%3E%7CDNAT%20%E5%88%B0%E6%B2%99%E7%AE%B1%20IP%3Cbr%2F%3E%E9%87%8D%E5%AE%9A%E5%90%91%7C%20D%5B%22TAP%20%E8%AE%BE%E5%A4%87%22%5D%0A%20%20%20%20D%20--%3E%20E%5B%22%E6%B2%99%E7%AE%B1%22%5D%0A"})]),fallback:d(()=>[...e[2]||(e[2]=[t(" Loading... ",-1)])]),_:1})),e[7]||(e[7]=i(`<p><code>from_envoy</code> 将目标 IP 从 Overlay 地址改写为沙箱内部 IP（<code>169.254.68.6</code>），源地址设为网关 IP（<code>169.254.68.5</code>），然后通过查找 <code>mvmip_to_ifindex</code> 将报文重定向到对应的 TAP 设备。</p><hr><h2 id="_3-会话跟踪" tabindex="-1">3. 会话跟踪 <a class="header-anchor" href="#_3-会话跟踪" aria-label="Permalink to &quot;3. 会话跟踪&quot;">​</a></h2><p>CubeVS 维护有状态的连接跟踪，以确保回复报文能够被正确地执行反向 NAT，同时系统能够检测和清理过期连接。</p><h3 id="_3-1-双-map-设计" tabindex="-1">3.1 双 Map 设计 <a class="header-anchor" href="#_3-1-双-map-设计" aria-label="Permalink to &quot;3.1 双 Map 设计&quot;">​</a></h3><p>两个 Map 协同工作：</p><ul><li><strong><code>egress_sessions</code></strong> 是主会话表。Key 为沙箱侧的原始五元组（沙箱 IP、目标 IP、沙箱端口、目标端口、协议，以及用于多版本支持的 version 字段）。Value 保存完整的 NAT 状态：SNAT IP 和端口、TAP ifindex、时间戳、TCP 状态以及主动关闭标志。</li><li><strong><code>ingress_sessions</code></strong> 是反向查找表。Key 为外部侧的五元组（外部 IP、节点 IP、外部端口、SNAT 端口、协议）。Value 仅存储足以重建 <code>egress_sessions</code> Key 的信息（沙箱 IP、沙箱端口、version）。</li></ul><p>这种双 Map 方案避免了在两个方向上重复存储完整的会话状态，同时仍能从连接的任意一端实现 O(1) 查找。</p><h3 id="_3-2-tcp-状态机" tabindex="-1">3.2 TCP 状态机 <a class="header-anchor" href="#_3-2-tcp-状态机" aria-label="Permalink to &quot;3.2 TCP 状态机&quot;">​</a></h3><p>CubeVS 实现了一个参照 Linux 内核 <code>nf_conntrack</code> 的 TCP 连接跟踪状态机，识别 11 种状态，包括 <code>SYN_SENT</code>、<code>SYN_RECV</code>、<code>ESTABLISHED</code>、<code>FIN_WAIT</code>、<code>CLOSE_WAIT</code>、<code>LAST_ACK</code>、<code>TIME_WAIT</code>、<code>CLOSE</code> 和 <code>SYN_SENT2</code>（同时打开场景）。状态转换由两个方向（original：沙箱到外部，reply：外部到沙箱）观察到的 TCP 标志驱动。</p><p>如此精细的跟踪有两个关键作用：</p><ol><li><strong>精确的超时控制</strong> —— <code>ESTABLISHED</code> 状态的会话可以安全存活数小时（默认 3 小时），而半打开的 <code>SYN_SENT</code> 会话应在 1 分钟后被回收。</li><li><strong>主动关闭检测</strong> —— 当沙箱发起关闭（发送第一个 FIN）时，会话进入 <code>TIME_WAIT</code> 状态并短暂保留以吸收重传。当外部侧发起关闭时，会话进入 <code>CLOSE_WAIT</code> / <code>LAST_ACK</code> 状态，超时时间更短。</li></ol><h3 id="_3-3-udp-和-icmp-跟踪" tabindex="-1">3.3 UDP 和 ICMP 跟踪 <a class="header-anchor" href="#_3-3-udp-和-icmp-跟踪" aria-label="Permalink to &quot;3.3 UDP 和 ICMP 跟踪&quot;">​</a></h3><p><strong>UDP</strong> 使用简单的两状态模型：</p><ul><li><code>UNREPLIED</code> —— 首个出站报文出现时设置。</li><li><code>REPLIED</code> —— 首个回复到达时设置。超时从 30 秒（未回复）延长至 180 秒（已回复）。</li></ul><p><strong>ICMP</strong> 同样使用两个状态（<code>UNREPLIED</code> / <code>REPLIED</code>），固定 30 秒超时。ICMP Echo 标识符用作会话 Key 中的&quot;端口&quot;，因此同一沙箱的并发 ping 请求能被独立跟踪。</p><h3 id="_3-4-会话回收器" tabindex="-1">3.4 会话回收器 <a class="header-anchor" href="#_3-4-会话回收器" aria-label="Permalink to &quot;3.4 会话回收器&quot;">​</a></h3><p>一个 Go 后台 goroutine 每 5 秒执行一次，遍历 <code>egress_sessions</code> Map。对于每个会话，它比较 <code>当前时间 - access_time</code> 与特定状态的超时值：</p><table tabindex="0"><thead><tr><th>协议</th><th>状态</th><th>超时</th></tr></thead><tbody><tr><td>TCP</td><td>SYN_SENT、SYN_RECV</td><td>1 分钟</td></tr><tr><td>TCP</td><td>ESTABLISHED</td><td>3 小时</td></tr><tr><td>TCP</td><td>FIN_WAIT、CLOSE_WAIT、LAST_ACK</td><td>1--2 分钟</td></tr><tr><td>TCP</td><td>TIME_WAIT、CLOSE</td><td>10 秒</td></tr><tr><td>UDP</td><td>UNREPLIED</td><td>30 秒</td></tr><tr><td>UDP</td><td>REPLIED</td><td>180 秒</td></tr><tr><td>ICMP</td><td>任意</td><td>30 秒</td></tr></tbody></table><p>当会话过期时，回收器同时删除 <code>egress_sessions</code> 和 <code>ingress_sessions</code> 中的条目。若会话不在正常终止状态（例如 <code>ESTABLISHED</code> 未经 FIN 即超时），回收器会记录告警。它还会监控会话数量，当占用率超过 Map 容量的 80% 时发出告警。</p><hr><h2 id="_4-snat-与-dnat" tabindex="-1">4. SNAT 与 DNAT <a class="header-anchor" href="#_4-snat-与-dnat" aria-label="Permalink to &quot;4. SNAT 与 DNAT&quot;">​</a></h2><h3 id="_4-1-snat-出站地址转换" tabindex="-1">4.1 SNAT：出站地址转换 <a class="header-anchor" href="#_4-1-snat-出站地址转换" aria-label="Permalink to &quot;4.1 SNAT：出站地址转换&quot;">​</a></h3><p>沙箱的每个出站报文在离开宿主机前，必须将源地址改写为可路由地址。CubeVS 在 <code>from_cube</code> 中使用最多四个 SNAT IP 的地址池完成此操作。</p><p><strong>IP 选择</strong>按沙箱确定性分配：<code>index = jhash(sandbox_ip) % 4</code>。这确保同一沙箱的所有连接使用相同的 SNAT IP，简化了外部防火墙规则和日志记录。</p><p><strong>端口分配</strong>使用逐 SNAT IP 条目的单调递增水位线。水位线从端口 30000 开始，每次新建连接时递增。到达 65535 时回绕。SNAT 条目受 BPF 自旋锁保护，确保不同 CPU 上的并发分配不会产生竞争。</p><p><strong>碰撞避免</strong>在插入时处理。选定端口后，BPF 程序尝试使用 <code>BPF_NOEXIST</code> 标志将反向查找条目插入 <code>ingress_sessions</code>。若 Key 已存在（表示另一会话已在相同外部端点上使用了该 SNAT IP:端口组合），分配器递增端口并重试，最多尝试 10 次。若 10 次均失败，报文将被丢弃。</p><p>分配成功后，<code>from_cube</code> 原地更新 IP 头（源 IP 和校验和）与传输层头（源端口和校验和），然后将报文重定向到宿主机网卡。</p><h3 id="_4-2-dnat-入站地址转换" tabindex="-1">4.2 DNAT：入站地址转换 <a class="header-anchor" href="#_4-2-dnat-入站地址转换" aria-label="Permalink to &quot;4.2 DNAT：入站地址转换&quot;">​</a></h3><p>DNAT 发生在三种场景中：</p><ol><li><p><strong>Overlay 流量</strong>（cube-dev 上的 <code>from_envoy</code>）—— 目标 IP 从 Overlay 地址改写为沙箱内部 IP（<code>169.254.68.6</code>），源地址设为网关地址（<code>169.254.68.5</code>）。报文被重定向到正确的 TAP 设备。</p></li><li><p><strong>会话回复流量</strong>（宿主机网卡上的 <code>from_world</code>）—— 目标 IP 和端口从节点的 SNAT 地址改写回沙箱的原始源地址和端口。<code>ingress_sessions</code> 中的反向查找提供了沙箱侧的坐标。</p></li><li><p><strong>端口映射流量</strong>（宿主机网卡上的 <code>from_world</code>）—— 对于通过端口映射暴露的服务，目标被改写为沙箱的监听端口。无需会话表参与，<code>remote_port_mapping</code> Map 直接提供转换。</p></li></ol><hr><h2 id="_5-网络策略" tabindex="-1">5. 网络策略 <a class="header-anchor" href="#_5-网络策略" aria-label="Permalink to &quot;5. 网络策略&quot;">​</a></h2><p>CubeVS 完全在内核态执行逐沙箱的出站网络策略，使用 LPM（最长前缀匹配）Trie 实现基于 CIDR 的规则。</p><h3 id="_5-1-架构" tabindex="-1">5.1 架构 <a class="header-anchor" href="#_5-1-架构" aria-label="Permalink to &quot;5.1 架构&quot;">​</a></h3><p>每个沙箱可以为其 TAP 设备关联两个 LPM Trie：</p><ul><li><strong><code>allow_out</code></strong> —— 目标 CIDR 的允许列表。若此 Map 存在且目标匹配，报文将被放行，不再检查拒绝列表。</li><li><strong><code>deny_out</code></strong> —— 目标 CIDR 的拒绝列表。若此 Map 存在且目标匹配（且未在允许列表中），报文将被丢弃。</li></ul><p>两者均实现为 Hash-of-Maps：外层 Map 以 TAP ifindex 为 Key，每个 Value 是内层 LPM Trie 的文件描述符。这种逐设备结构意味着一个沙箱的策略更新无需遍历或锁定其他沙箱的 Map。</p><h3 id="_5-2-评估顺序" tabindex="-1">5.2 评估顺序 <a class="header-anchor" href="#_5-2-评估顺序" aria-label="Permalink to &quot;5.2 评估顺序&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. 目标是否为沙箱网关（169.254.68.5）？</span></span>
<span class="line"><span>   --&gt; 是：放行（内部流量，始终允许）</span></span>
<span class="line"><span>   </span></span>
<span class="line"><span>2. 沙箱是否有 allow_out Map，且目标是否匹配？</span></span>
<span class="line"><span>   --&gt; 是：放行</span></span>
<span class="line"><span>   </span></span>
<span class="line"><span>3. 沙箱是否有 deny_out Map，且目标是否匹配？</span></span>
<span class="line"><span>   --&gt; 是：丢弃</span></span>
<span class="line"><span>   </span></span>
<span class="line"><span>4. 默认：放行</span></span></code></pre></div><p>优先级为：<strong>允许 &gt; 拒绝 &gt; 默认放行</strong>。这意味着沙箱运维人员可以设置宽泛的拒绝规则（例如 <code>0.0.0.0/0</code> 阻止所有互联网访问），然后通过允许规则开放特定白名单。</p><h3 id="_5-3-始终拒绝的-cidr" tabindex="-1">5.3 始终拒绝的 CIDR <a class="header-anchor" href="#_5-3-始终拒绝的-cidr" aria-label="Permalink to &quot;5.3 始终拒绝的 CIDR&quot;">​</a></h3><p>无论策略如何配置，CubeVS 都会阻止沙箱访问私有和链路本地地址段：</p><ul><li><code>10.0.0.0/8</code></li><li><code>127.0.0.0/8</code></li><li><code>169.254.0.0/16</code></li><li><code>172.16.0.0/12</code></li><li><code>192.168.0.0/16</code></li></ul><p>这些地址段在 <code>AddTAPDevice()</code> 期间被添加到拒绝列表，且无法被允许规则覆盖。这确保了沙箱无法探测宿主机的内部网络或其他沙箱的链路本地地址。</p><h3 id="_5-4-策略配置" tabindex="-1">5.4 策略配置 <a class="header-anchor" href="#_5-4-策略配置" aria-label="Permalink to &quot;5.4 策略配置&quot;">​</a></h3><p>注册 TAP 设备时，调用方提供 <code>MVMOptions</code> 结构体：</p><ul><li><strong><code>AllowInternetAccess</code></strong> —— 若为 <code>false</code>，安装全面拒绝规则（<code>0.0.0.0/0</code>）。</li><li><strong><code>AllowOut</code></strong> —— 需添加到允许列表的 CIDR 列表。</li><li><strong><code>DenyOut</code></strong> —— 需添加到拒绝列表的 CIDR 列表。</li></ul><p>策略可在运行时通过修改内层 LPM Trie 进行更新，无需卸载或重新加载 BPF 程序。</p><hr><h2 id="_6-端口映射" tabindex="-1">6. 端口映射 <a class="header-anchor" href="#_6-端口映射" aria-label="Permalink to &quot;6. 端口映射&quot;">​</a></h2><p>沙箱无法从宿主机外部直接访问。当沙箱需要暴露服务（如 HTTP 服务器）时，CubeVS 提供端口映射 —— 一条静态 NAT 规则，将到达宿主机特定端口的流量转发到沙箱的特定端口。</p><h3 id="_6-1-双向映射" tabindex="-1">6.1 双向映射 <a class="header-anchor" href="#_6-1-双向映射" aria-label="Permalink to &quot;6.1 双向映射&quot;">​</a></h3><p>两个 BPF Map 支撑端口映射：</p><ul><li><strong><code>remote_port_mapping</code></strong> 将宿主端口映射到（TAP ifindex，沙箱监听端口）。<code>from_world</code> 用它将入站连接路由到正确的沙箱。</li><li><strong><code>local_port_mapping</code></strong> 将（TAP ifindex，沙箱监听端口）映射回宿主端口。<code>from_cube</code> 将其用作优化：当沙箱从已映射的监听端口发送报文时，过滤器可跳过完整的 NAT 会话创建，直接将报文 SNAT 到节点 IP 的正确端口并重定向到宿主机网卡。</li></ul><h3 id="_6-2-入站代理流程" tabindex="-1">6.2 入站代理流程 <a class="header-anchor" href="#_6-2-入站代理流程" aria-label="Permalink to &quot;6.2 入站代理流程&quot;">​</a></h3><ol><li>外部客户端发送报文到 <code>node_ip:host_port</code>。</li><li><code>from_world</code> 查找 <code>remote_port_mapping[host_port]</code>。</li><li>若匹配，过滤器将目标改写为 <code>169.254.68.6:sandbox_listen_port</code> 并重定向到 TAP 设备。</li><li>沙箱在其监听端口上收到连接。</li></ol><p>此路径不会在会话表中创建条目，使 Map 在长连接服务场景下保持精简。</p><h3 id="_6-3-管理" tabindex="-1">6.3 管理 <a class="header-anchor" href="#_6-3-管理" aria-label="Permalink to &quot;6.3 管理&quot;">​</a></h3><p>Go API 提供了 <code>AddPortMapping()</code>、<code>DelPortMapping()</code>、<code>ListPortMapping()</code> 和 <code>GetPortMapping()</code>，用于在运行时管理映射关系。</p><hr><h2 id="_7-tap-设备生命周期" tabindex="-1">7. TAP 设备生命周期 <a class="header-anchor" href="#_7-tap-设备生命周期" aria-label="Permalink to &quot;7. TAP 设备生命周期&quot;">​</a></h2><p>每个沙箱获得一个专用的 TAP 设备，作为其在宿主侧的唯一网络接口。CubeVS 管理这些设备的完整生命周期。</p><h3 id="_7-1-注册-addtapdevice" tabindex="-1">7.1 注册：AddTAPDevice <a class="header-anchor" href="#_7-1-注册-addtapdevice" aria-label="Permalink to &quot;7.1 注册：AddTAPDevice&quot;">​</a></h3><p>当沙箱运行时（Cubelet）创建新沙箱时，调用 <code>AddTAPDevice(ifindex, ip, id, version, options)</code>：</p><ol><li>沙箱元数据（IP、UUID、版本）写入 <code>ifindex_to_mvmmeta</code>。</li><li>IP 到设备的映射写入 <code>mvmip_to_ifindex</code>。</li><li>根据 <code>MVMOptions</code> 初始化网络策略内层 Map —— 首先安装始终拒绝的 CIDR，然后是调用方指定的允许或拒绝规则。</li></ol><h3 id="_7-2-过滤器挂载-attachfilter" tabindex="-1">7.2 过滤器挂载：AttachFilter <a class="header-anchor" href="#_7-2-过滤器挂载-attachfilter" aria-label="Permalink to &quot;7.2 过滤器挂载：AttachFilter&quot;">​</a></h3><p>TAP 设备在操作系统层面创建后，<code>AttachFilter(ifindex)</code> 加载已固定的 <code>from_cube</code> BPF 程序并完成挂载：</p><ol><li>在 TAP 设备上创建 clsact qdisc（若不存在）。</li><li>将 <code>from_cube</code> TC 过滤器挂载到 qdisc 的 ingress hook。</li><li>创建 <code>allow_out</code> 和 <code>deny_out</code> 的内层 LPM Trie 并链接到外层 Hash-of-Maps。</li></ol><p>从此刻起，沙箱发送的每个报文都会被 <code>from_cube</code> 拦截。</p><h3 id="_7-3-清理-deltapdevice" tabindex="-1">7.3 清理：DelTAPDevice <a class="header-anchor" href="#_7-3-清理-deltapdevice" aria-label="Permalink to &quot;7.3 清理：DelTAPDevice&quot;">​</a></h3><p>沙箱销毁时，<code>DelTAPDevice(ifindex, ip)</code> 执行：</p><ol><li>移除网络策略内层 Map（从 Hash-of-Maps 中删除内层 LPM Trie）。</li><li>删除 <code>ifindex_to_mvmmeta</code> 和 <code>mvmip_to_ifindex</code> 中的条目。</li><li>引用此 TAP 设备的活跃会话保留在原处 —— 会话回收器会在超时后清理它们。这避免了在销毁时执行代价高昂的全 Map 扫描。</li></ol><hr><h2 id="_8-初始化" tabindex="-1">8. 初始化 <a class="header-anchor" href="#_8-初始化" aria-label="Permalink to &quot;8. 初始化&quot;">​</a></h2><h3 id="_8-1-init-流程" tabindex="-1">8.1 Init() 流程 <a class="header-anchor" href="#_8-1-init-流程" aria-label="Permalink to &quot;8.1 Init() 流程&quot;">​</a></h3><p><code>Init()</code> 在网络代理启动时调用一次，执行创建任何沙箱之前必须完成的系统级设置：</p><ol><li><p><strong>加载 BPF 对象</strong> —— 三个目标文件（localgw、mvmtap、nodenic）被加载到内核。加载前，Go 库在 BPF 字节码中改写编译期常量为当前宿主机的实际值：</p><ul><li>沙箱内部 IP（<code>169.254.68.6</code>）和网关 IP（<code>169.254.68.5</code>）</li><li>沙箱 MAC 地址</li><li>cube-dev 接口索引、IP 和 MAC</li><li>宿主机网卡接口索引、IP、MAC 和下一跳网关 MAC</li></ul></li><li><p><strong>固定程序和 Map</strong> —— 所有程序和 Map 被固定在 <code>/sys/fs/bpf/</code> 下，使后续加载的程序（逐 TAP 过滤器）能通过文件系统路径访问共享 Map。</p></li><li><p><strong>挂载 TC 过滤器</strong> ——</p><ul><li><code>from_envoy</code> 挂载到 cube-dev 的 TC egress hook（处理 Overlay 到沙箱的流量）。</li><li><code>from_world</code> 挂载到宿主机网卡的 TC ingress hook（处理外部到沙箱的回复流量）。</li><li><code>from_world</code> 也挂载到回环接口以确保完备性。</li></ul></li><li><p><strong>挂载 XDP</strong> —— <code>filter_from_cube</code> 挂载到 cube-dev 的 XDP hook。这提供了对未经请求的入站 TCP/UDP 流量的早期高性能拒绝，使其在到达 TC 层之前即被丢弃。</p></li></ol><h3 id="_8-2-常量改写" tabindex="-1">8.2 常量改写 <a class="header-anchor" href="#_8-2-常量改写" aria-label="Permalink to &quot;8.2 常量改写&quot;">​</a></h3><p>BPF 程序无法在运行时读取配置文件。CubeVS 采用了 eBPF 生态中常见的模式：BPF C 源码中的全局变量编译为常量，Go 加载器在将 ELF 对象加载到内核之前改写这些值。这种方式兼具编译期常量的性能优势（验证器可优化分支）和运行时配置的灵活性。</p><hr><h2 id="_9-arp-代理" tabindex="-1">9. ARP 代理 <a class="header-anchor" href="#_9-arp-代理" aria-label="Permalink to &quot;9. ARP 代理&quot;">​</a></h2><p>沙箱被分配链路本地 IP（<code>169.254.68.6</code>），默认网关为 <code>169.254.68.5</code>。由于这些地址仅存在于 TAP 设备的点对点链路内，<code>169.254.68.5</code> 处并没有真实的主机来响应 ARP 请求。CubeVS 通过内置于 <code>from_cube</code> 过滤器中的 ARP 代理解决这个问题。</p><h3 id="_9-1-工作原理" tabindex="-1">9.1 工作原理 <a class="header-anchor" href="#_9-1-工作原理" aria-label="Permalink to &quot;9.1 工作原理&quot;">​</a></h3><ol><li>沙箱网络栈发送 ARP 请求：<em>&quot;谁是 169.254.68.5？请告诉 169.254.68.6。&quot;</em></li><li><code>from_cube</code> 检测到 ARP 请求，交换发送方和目标 IP 地址构造 ARP 回复，并将发送方 MAC 填写为 cube-dev 网关 MAC 地址。</li><li>回复沿同一 TAP 设备的出方向重定向回沙箱，如同真实网关作出了应答。</li></ol><p>此时沙箱拥有了默认网关的有效 ARP 条目，可以向任意目标发送 IP 报文。这些报文经由 TAP 路由，被 <code>from_cube</code> 拦截，执行 SNAT 后转发到宿主机网卡 —— 完成第 2.1 节所述的出站路径。</p><h3 id="_9-2-为什么需要-arp-代理" tabindex="-1">9.2 为什么需要 ARP 代理 <a class="header-anchor" href="#_9-2-为什么需要-arp-代理" aria-label="Permalink to &quot;9.2 为什么需要 ARP 代理&quot;">​</a></h3><p>在传统的网桥方案中，ARP 由网桥将请求转发到所有端口来解析。CubeVS 不使用网桥，每个 TAP 都是独立的点对点链路。ARP 代理是使 IP 路由在此链路上正常工作的最小机制，无需引入任何广播域或共享的二层网段。</p><hr><h2 id="_10-总结" tabindex="-1">10. 总结 <a class="header-anchor" href="#_10-总结" aria-label="Permalink to &quot;10. 总结&quot;">​</a></h2><p>CubeVS 通过三层 eBPF 架构实现沙箱网络隔离：</p>`,91)),(a(),l(n,null,{default:d(()=>[c(r,{id:"mermaid-1010",class:"mermaid",graph:"graph%20TB%0A%20%20%20%20subgraph%20%22Overlay%20%E7%BD%91%E7%BB%9C%22%0A%20%20%20%20%20%20%20%20OV%5B%22Envoy%20%2F%20Overlay%22%5D%0A%20%20%20%20end%0A%20%20%20%20%0A%20%20%20%20subgraph%20%22%E5%AE%BF%E4%B8%BB%E6%9C%BA%E5%86%85%E6%A0%B8%22%0A%20%20%20%20%20%20%20%20CD%5B%22cube-dev%3Cbr%2F%3E(from_envoy%20%2B%20XDP%20%E8%BF%87%E6%BB%A4%E5%99%A8)%22%5D%0A%20%20%20%20%20%20%20%20NIC%5B%22%E5%AE%BF%E4%B8%BB%E6%9C%BA%E7%BD%91%E5%8D%A1%20-%20eth0%3Cbr%2F%3E(from_world)%22%5D%0A%20%20%20%20end%0A%20%20%20%20%0A%20%20%20%20subgraph%20%22%E6%B2%99%E7%AE%B1%20TAP%20%E8%AE%BE%E5%A4%87%22%0A%20%20%20%20%20%20%20%20T1%5B%22TAP%201%3Cbr%2F%3E(from_cube)%22%5D%0A%20%20%20%20%20%20%20%20T2%5B%22TAP%202%3Cbr%2F%3E(from_cube)%22%5D%0A%20%20%20%20%20%20%20%20T3%5B%22TAP%20N%3Cbr%2F%3E(from_cube)%22%5D%0A%20%20%20%20end%0A%20%20%20%20%0A%20%20%20%20subgraph%20%22%E6%B2%99%E7%AE%B1%22%0A%20%20%20%20%20%20%20%20S1%5B%22%E6%B2%99%E7%AE%B1%201%22%5D%0A%20%20%20%20%20%20%20%20S2%5B%22%E6%B2%99%E7%AE%B1%202%22%5D%0A%20%20%20%20%20%20%20%20S3%5B%22%E6%B2%99%E7%AE%B1%20N%22%5D%0A%20%20%20%20end%0A%20%20%20%20%0A%20%20%20%20OV%20--%3E%20CD%0A%20%20%20%20CD%20--%3E%20T1%20%26%20T2%20%26%20T3%0A%20%20%20%20T1%20--%3E%20S1%0A%20%20%20%20T2%20--%3E%20S2%0A%20%20%20%20T3%20--%3E%20S3%0A%20%20%20%20S1%20--%3E%20T1%0A%20%20%20%20S2%20--%3E%20T2%0A%20%20%20%20S3%20--%3E%20T3%0A%20%20%20%20T1%20%26%20T2%20%26%20T3%20--%3E%7CSNAT%7C%20NIC%0A%20%20%20%20NIC%20--%3E%7C%E5%8F%8D%E5%90%91%20NAT%7C%20T1%20%26%20T2%20%26%20T3%0A%20%20%20%20NIC%20--%3E%20EXT%5B%22%E5%A4%96%E9%83%A8%E7%BD%91%E7%BB%9C%22%5D%0A%20%20%20%20EXT%20--%3E%20NIC%0A"})]),fallback:d(()=>[...e[3]||(e[3]=[t(" Loading... ",-1)])]),_:1})),e[8]||(e[8]=o("p",null,"核心设计原则：",-1)),e[9]||(e[9]=o("ul",null,[o("li",null,[o("strong",null,"所有数据面逻辑在内核态执行"),t(" —— 策略评估、NAT、会话跟踪和 ARP 解析均在 eBPF 中完成，无需用户态上下文切换。")]),o("li",null,[o("strong",null,"逐沙箱隔离"),t(" —— 每个沙箱拥有专属的 TAP 设备、独立的策略 Trie 和独立的会话。没有共享网桥或交换机。")]),o("li",null,[o("strong",null,"有状态的连接跟踪"),t(" —— 双 Map 会话模型（egress + ingress）实现双向 NAT，O(1) 查找，精确的协议感知超时。")]),o("li",null,[o("strong",null,"极简控制面"),t(" —— Go 库负责初始化、设备生命周期和定期会话清理。稳态数据路径完全在 eBPF 中运行。")])],-1))])}const m=s(h,[["render",P]]);export{C as __pageData,m as default};
